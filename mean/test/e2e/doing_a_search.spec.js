describe('doing a search', function(){
	it('goes to homepage and does a search',
	function(){
		// go to homepage
		browser.get('http://localhost:3000');

		// fill in a filter

		// hit the search button

		// user should now see filings,
		// properly filtered
	});
});
	

// The convention is to set the NODE_ENV
// environment variable to "production"
// when in production and check for it
// inside this "config.js" file.
module.exports = {
	db_url: 'mongodb://localhost/formd'
};
